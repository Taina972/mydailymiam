<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href={{ path("home") }}><img src="{{ asset('assets/images/logo-nav.png') }}" alt="Logo MDM"></a>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarScroll" aria-controls="navbarScroll" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>

    <div class="collapse navbar-collapse" id="navbarScroll">
        <ul class="navbar-nav mr-auto my-2 my-lg-0 navbar-nav-scroll" style="max-height: 100px;">
            <li class="nav-item active">
                <a class="nav-link" href={{ path("home") }}>Accueil <span class="sr-only"></span></a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href={{ path('recette_user') }}>Recettes <span class="sr-only"></span></a>
            </li>
            
            <li class="nav-item">
                <a class="nav-link" href={{ path('article_user') }}>Articles <span class="sr-only"></span></a>
            </li> 

            {% if app.user%}

                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Accès à mon compte</a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href={{ path('compte_user', {'id': app.user.id}) }}>Mon compte</a>
                        <a class="dropdown-item" href={{ path('app_login') }}>Connexion</a>
                        <a class="dropdown-item" href={{ path('app_register') }}>Inscription</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href={{ path('app_logout') }}>Déconnexion</a>
                    </div>
                </li> 
            {% endif %}
            {# <li class="nav-item dropdown">
             <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Connexion</a>
                <div class="dropdown-menu">
                    <form class="px-4 py-3">
                        <div class="form-group">
                            <label for="exampleDropdownFormEmail1">Adresse e-mail</label>
                            <input type="email" class="form-control" id="exampleDropdownFormEmail1" placeholder="email@exemple.com">
                        </div>
                        <div class="form-group">
                            <label for="exampleDropdownFormPassword1">Mot de passe</label>
                            <input type="password" class="form-control" id="exampleDropdownFormPassword1" placeholder="Mot de passe">
                        </div>
                        {# <div class="form-check">
                            <input type="checkbox" class="form-check-input" id="dropdownCheck">
                            {# <label class="form-check-label" for="dropdownCheck"> Se rappeler de moi</label> #}
                        {# </div> #}
                        {# <button type="submit" class="btn btn-primary" href={{ path('app_login') }}>Connexion</button>
                    </form>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href={{ path('app_register') }}>Pas encore de compte ? Inscrivez-vous ici</a>
                    {# <a class="dropdown-item" href="#">Mot de passe oublié ?</a> #}
                    {# <button type="submit" class="btn btn-primary" href={{ path('app_logout') }}>Déconnexion</button>
                </div>
            </li>  #}

            <li class="nav-item">
                <a class="nav-link" href={{ path('contact') }}>Contact<span class="sr-only"></span></a>
            </li> 


            {% if app.user %}
                {% if app.user.roles[0] is same as "ROLE_ADMIN" %}
                    <li class="nav-item">
                        <a class="nav-link" href={{ path('admin_home') }}>Administration<span class="sr-only"></span></a>
                    </li> 
                {% endif %}
            {% endif %}
        
        </ul>
    </div>

    <form class="d-flex">
        <input class="form-control mr-2" type="search" placeholder="Rechercher sur le site" aria-label="Rechercher">
        <button class="btn btn-outline-danger" type="submit">Rechercher</button>
    </form>
               
</nav>